*** MRF motion estimation and correction method with scout-based pattern matching (SPAM). ***

This repo contains the following functionalitites: 
- quantitative scout and navigator images reconstruction, 
- motion-dictionary generation and compression, 
- motion estimation using pattern matching of the spiral navigator signals, 
- motion correction on full-res MRF data.

